name: wellbeingx
description: "WellbeingX — offline-first digital wellbeing & focus."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.6.1

  drift: ^2.20.3
  drift_flutter: ^0.2.4
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.0

  go_router: 14.6.2

  fl_chart: ^0.69.2
  google_fonts: ^6.2.1

  intl: ^0.19.0
  equatable: ^2.0.5
  collection: ^1.18.0

  shared_preferences: ^2.3.3
  permission_handler: ^11.3.1
  flutter_local_notifications: ^17.2.4
  timezone: ^0.9.4
  share_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  drift_dev: ^2.20.3
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
